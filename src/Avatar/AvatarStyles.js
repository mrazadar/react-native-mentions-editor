import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrapper: {
    width: 40,
    height: 40,
    borderRadius: 50,
    marginRight: 5,
    alignItems: "center",
    justifyContent: "center"
  },
  name: {
    fontWeight: "bold",
    fontSize: 16,
    color: "#fff"
  }
});
